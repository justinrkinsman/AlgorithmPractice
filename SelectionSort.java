public class SelectionSort { // O(n^2) even in the best case (n-1) + (n-2) + (n-3) + (n-4)....
    /*
     * Place pointer at first element. Find the smallest index of the remaining
     * indices.
     * Swap it with current pointer. Repeat.
     */
}
